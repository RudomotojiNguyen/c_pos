part of 'discount_dialog.dart';

class CouponItemLoading extends StatelessWidget {
  const CouponItemLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const XPlaceHolder(
      width: 120,
      height: 120,
    );
  }
}
